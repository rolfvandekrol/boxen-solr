<?xml version="1.0" encoding="UTF-8"?>
<solr persistent="false">
  <cores adminPath="/admin/cores">
    <% instances.each do |inst| %>
        <core name="<%= $inst %>" instanceDir="<%= $inst %>" />
    <% end %>
  </cores>
</solr>